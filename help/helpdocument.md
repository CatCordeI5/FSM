1. SYNTAX (`~o com ~`)
In FSM, we do not use standard commands. We utilize the **LadG Protocol**.

Structure: `~o com ~ [COMMAND] [ARGUMENT]`

2. SPELL LIST (COMMANDS)

| Spell | Function |
| :--- | :--- |
| `~o com ~ reveal truth` | Displays the list of available protocols. |
| `~o com ~ clear` | Purges digital traces from the terminal screen. |

| Spell | Function |
| :--- | :--- |
| `~o com ~ sense ether` | Scans raw radio spectrum in your vicinity. |
| `~o com ~ pull [LIB_NAME]` | Pulls data from the void (internet) into RAM Disk. |

| Spell | Function |
| :--- | :--- |
| `~o com ~ loom [FILE.adl]` | Weaves ADL0- code into a binary soul. |
| `~o com ~ exec [FILE.bin]` | Executes the binary soul within the kernel. |

| Spell | Function |
| :--- | :--- |
| `~o com ~ shifthat on` | Activates kernel-level protection. |
| `~o com ~ shifthat scan` | Scans for suspicious processes within 2-5 seconds. |

Your QWERTY keyboard is a coordinate map. Use modifier keys to move your "Virtual Finger":

*   `&` = Right
*   `+` = Left
*   `*` = Proceed/Up
*   `l` = Lower/Down

Spawn Point System:
*   `~o set spawn point check {[X]} ~` : Anchors character X as a reference point.
*   `~del set spawn point check {[X]} ~` : Removes the specific anchor.

4. MESSAGE FROM THE CREATOR
*"Others found diamonds and made them easy to find. We found emeralds and hid them in depths reachable only by those willing to suffer for knowledge."*
